import React from 'react';
import TopNavigation from './TopNavigation';

export default {
  title: 'components/TopNavigation',
  component: TopNavigation,
};

const Template = () => <TopNavigation />;

export const Default = Template.bind({});
